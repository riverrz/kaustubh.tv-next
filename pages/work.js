import styled from "styled-components";
import Layout from "../components/Layout";

const Work = props => {
  return (
    <Layout>
      <h1>Try</h1>
    </Layout>
  );
};

export default styled(Work)``;
